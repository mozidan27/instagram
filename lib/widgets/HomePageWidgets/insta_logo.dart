import 'package:flutter/material.dart';

class InstaLogoClick extends StatelessWidget {
  const InstaLogoClick({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
